{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command // \"\"' 2>/dev/null); if echo \"$cmd\" | grep -q 'git push'; then echo '{\"systemMessage\": \"⚠️  Reminder: have you synced from upstream (ZhixiangLuo/10xProductivity) recently? If not: git fetch upstream && git merge upstream/main\"}'; fi"
          }
        ]
      }
    ]
  }
}
